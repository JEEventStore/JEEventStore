language: java
jdk:
  - oraclejdk7
  - openjdk7
  - openjdk6
env:
  matrix:
    - MAVEN_PROFILE=arq-glassfish-embedded

script: "mvn clean install test-compile surefire:test -P${MAVEN_PROFILE}"

branches:
  only:
    - master
